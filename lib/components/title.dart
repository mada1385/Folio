import 'package:flutter/material.dart';
import 'package:folio/components/texts.dart';

class Tiltle extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Padding(
        padding: EdgeInsets.only(right: 20, left: 20), child: k_title);
  }
}
